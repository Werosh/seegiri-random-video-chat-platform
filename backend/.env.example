NODE_ENV=development
PORT=4000
LOG_LEVEL=info

# Comma-separated origins for CORS + Socket.IO (e.g. http://localhost:3000)
SEEGIRI_PUBLIC_ORIGINS=http://localhost:3000

# Optional: Firebase Admin (Phase 7 -leave empty for local dev)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
