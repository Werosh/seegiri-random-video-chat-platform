/**
 * WebRTC utilities will be implemented in Phase 4.
 * Exporting a stable module boundary keeps imports consistent as the stack grows.
 */
export type MediaDeviceKind = "audioinput" | "videoinput";
