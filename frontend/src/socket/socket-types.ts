/**
 * Socket event contracts will be defined in Phase 3.
 * Keep imports centralized here to avoid stringly-typed emits across the app.
 */
export type SocketNamespace = "/";

export type ClientSocketEvents = Record<string, never>;
export type ServerSocketEvents = Record<string, never>;
